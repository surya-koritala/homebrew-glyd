# Homebrew tap for Glyd

    brew install surya-koritala/glyd/glyd

Installs the `glyd` CLI (the codec, Apache-2.0), the `glyd-store` CLI
(the store, BUSL-1.1) and `include/glyd.h`, built from the tagged
release of https://github.com/surya-koritala/Glyd.
